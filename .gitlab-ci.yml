stages:
  - test

cache:
  key: "$CI_PROJECT_NAME"
  paths:
    - $HOME/.cache/pip

before_script:
  - pip install tox

test_python36:
  stage: test
  image: python:3.6
  script: tox --skip-missing-interpreters

test_pypy3:
  stage: test
  image: pypy:3-5
  script: tox --skip-missing-interpreters
